/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      './index.html',
      './src/**/*.{js,ts,jsx,tsx}',
    ],
    theme: {
      extend: {
        // Add custom theme values here if needed
      },
    },
    plugins: [
      // e.g. require('@tailwindcss/forms'),
    ],
  };
  